# `@/engine`

The **zero-React, framework-agnostic** stream-execution kernel: Spliterator pull, Sink chain,
event-log emission, and the parallel scheduler. Lands in S0.3–S0.5. Nothing here may import
React / Next / Zustand (import boundary enforced from S0.7).
